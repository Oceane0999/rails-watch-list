<h1>Your Lists</h1>

<p><%= link_to 'Create a New List', new_list_path %></p>

<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %><%= link_to 'Delete', list_path(list), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
  <% end %>
</ul>
